const DEFAULT_OPPORTUNITIES = [
  {
    id: "call-for-applications-pibbss-winter-fellowship-program-20262027-in-south-africa-3000-monthly-stipend-free-accommodation-flights-and-mentorship",
    title: "Call For Applications: PIBBSS Winter Fellowship Program 2026–2027 in South Africa ($3,000 Monthly Stipend, Free Accommodation, Flights, and Mentorship)",
    category: "Fellowships",
    company: "PIBBSS",
    location: "South Africa (Hybrid / Onsite)",
    country: "South Africa",
    remote: "Onsite",
    date: "2026-07-08",
    deadline: "2026-10-31",
    image: "img/opp_call-for-applications-pibbss-w.jpg",
    shortDescription: "Applications are now open for the PIBBSS Winter Fellowship Program 2026–2027, an intensive research fellowship hosted by the Principles of Intelligent...",
    description: "Applications are now open for the PIBBSS Winter Fellowship Program 2026–2027, an intensive research fellowship hosted by the Principles of Intelligent Behavior in Biological and Social Systems (PIBBSS) initiative in South Africa. The fully funded programme brings together exceptional students and early-career researchers from around the world to explore the principles of intelligence across biological, artificial, and social systems. Fellows will participate in interdisciplinary lectures, research collaborations, workshops, and mentorship sessions with leading scientists and innovators. (princint.ai) Fellowship Benefits Successful applicants will receive: Monthly stipend of USD 3,000 Round-trip international airfare Free accommodation throughout the fellowship Meals during the programme Access to world-class lectures and workshops Mentorship from leading researchers and academics Opportunities for interdisciplinary research collaboration Networking with an international cohort of fellows. Who Can Apply? The fellowship is open to: Undergraduate students Master&#8217;s students PhD students Postdoctoral researchers Early-career professionals with a strong interest in intelligence research Applicants from all countries are encouraged to apply. Candidates should demonstrate academic excellence and an interest in interdisciplinary research spanning biology, artificial intelligence, neuroscience, cognitive science, mathematics, physics, computer science, economics, or related fields. Programme Focus The fellowship explores topics including: Artificial Intelligence Machine Learning Neuroscience Cognitive Science Complex Systems Evolutionary Biology Mathematics Physics Economics Social Systems Computational Science Collective Intelligence. Why This Matters The PIBBSS Winter Fellowship provides emerging researchers with a unique opportunity to collaborate across disciplines, gain mentorship from internationally recognized experts, and contribute to cutting-edge research on intelligence. By bringing together participants from diverse academic backgrounds, the programme aims to foster innovative ideas that address complex scientific and societal challenges. Application Deadline August 31, 2026.Advertisement (adsbygoogle = window.adsbygoogle || []).push({}); Fellowship Dates The Winter Fellowship will take place in South Africa from June to August 2027. Specific programme dates will be communicated to selected fellows. How To Apply Interested applicants can learn more and submit their applications through the official fellowship portal: https://princint.ai/programs/fellowship/ ShareRelated Posts:Ultimate 2026 Guide to the Top Business Enabling&hellip;MSME Africa Unveils Top 50 Remarkable MSME Founders&hellip;Overcoming Binding Constraints to Competitive&hellip;15 Social Media Management Tools That Will Make Life&hellip;#IWD2023: Celebrating Outstanding Women in Micro&hellip;Call For Applications: AstraZeneca Young Health&hellip; Related Topics:Academic ResearchArtificial IntelligenceCognitive ScienceEarly Career ResearchersFully Funded FellowshipGlobal OpportunitiesInternational FellowshipMachine Learning.Mentorship ProgramNeurosciencePhD OpportunitiesPIBBSS Winter FellowshipResearch FellowshipSouth Africa FellowshipSTEM Fellowship Up Next 46 Microfinance Banks Lose CBN Licences—Here&#8217;s What Went Wrong\n\nCall For Applications: AGFUND Development Programme 2026 for Diana Award Recipients in Africa",
    requirements: ["Round-trip international airfare","Meals during the programme","Access to world-class lectures and workshops","Opportunities for interdisciplinary research collaboration"],
    benefits: ["Monthly stipend of USD 3,000","Free accommodation throughout the fellowship","Mentorship from leading researchers and academics"],
    skills: ["Leadership","Problem Solving"],
    experienceLevel: "Graduate",
    applyUrl: "https://princint.ai/programs/fellowship/",
    featured: true,
    trending: true,
    status: "published"
  },
  {
    id: "call-for-applications-agfund-development-programme-2026-for-diana-award-recipients-in-africa",
    title: "Call For Applications: AGFUND Development Programme 2026 for Diana Award Recipients in Africa",
    category: "Business Funding",
    company: "AGFUND",
    location: "South Africa (Online / Remote)",
    country: "South Africa",
    remote: "Remote",
    date: "2026-07-08",
    deadline: "2026-10-31",
    image: "img/opp_call-for-applications-agfund-d.jpg",
    shortDescription: "Applications are now open for the AGFUND Development Programme 2026, a six-month leadership and capacity-building programme designed to support Diana ...",
    description: "Applications are now open for the AGFUND Development Programme 2026, a six-month leadership and capacity-building programme designed to support Diana Award Recipients across Africa in strengthening, sustaining, and scaling their social impact initiatives. The programme is delivered through a partnership between the Arab Gulf Programme for Development (AGFUND) and The Diana Award. It combines expert-led learning, peer collaboration, and tailored development support to help young changemakers expand the impact of their social action projects. Programme Benefits Selected participants will receive: Six months of expert-led leadership and capacity-building sessions Personalized development support Peer learning and collaboration with fellow changemakers across Africa Opportunities to strengthen and scale social impact initiatives Access to a growing network of young leaders and development partners Professional and personal development opportunities. Who Can Apply? Applicants must: Be a Diana Award Recipient Be based in Africa Be between 16 and 25 years old at the start of the programme Be actively leading a social action project, initiative, or campaign Be seeking to strengthen, sustain, or grow their impact Be able to commit to the full six-month virtual programme. Programme Timeline Applications Open: July 2, 2026 Application Deadline: July 31, 2026 (11:59 p.m. GMT) Participant Onboarding: September 2026 Programme Launch: October 2026. Why This Matters Young social innovators often need structured support to sustain and expand the impact of their initiatives. The AGFUND Development Programme equips Diana Award Recipients with practical leadership skills, strategic guidance, and a collaborative network that enables them to build stronger, more sustainable social impact projects across Africa. How To Apply Interested and eligible applicants can learn more and submit their applications through the official programme page: https://diana-award.org.uk/our-programmes-and-initiatives/award-and-development/development-opportunitiesAdvertisement (adsbygoogle = window.adsbygoogle || []).push({}); &nbsp; ShareRelated Posts:MSME Africa Unveils Top 50 Remarkable MSME Founders&hellip;Ultimate 2026 Guide to the Top Business Enabling&hellip;AAAID holds webinar to discuss ‘Financing Small and&hellip;Overcoming Binding Constraints to Competitive&hellip;15 Social Media Management Tools That Will Make Life&hellip;#IWD2023: Celebrating Outstanding Women in Micro&hellip; Related Topics:Africa Leadership ProgrammeAfrica opportunitiesAfrican YouthAGFUND Development ProgrammeCAPACITY BUILDINGCommunity developmentDevelopment ProgrammeFellowshipLeadership TrainingNonprofit LeadersSocial ImpactSocial InnovationThe Diana AwardYoung ChangemakersYouth Leadership Up Next Call For Applications: PIBBSS Winter Fellowship Program 2026–2027 in South Africa ($3,000 Monthly Stipend, Free Accommodation, Flights, and Mentorship)\n\nCall for Applications: UNDP African Youth Co-Creators Council 2026",
    requirements: ["Six months of expert-led leadership and capacity-building sessions","Personalized development support","Peer learning and collaboration with fellow changemakers across Africa","Opportunities to strengthen and scale social impact initiatives"],
    benefits: ["Be a Diana Award Recipient","Access to world-class mentorship and business coaching.","Opportunity to network with peers and global industry experts.","Financial support, stipend, or equity-free grants (where applicable)."],
    skills: ["Pitching","Business Development","Financial Strategy"],
    experienceLevel: "Graduate",
    applyUrl: "https://diana-award.org.uk/our-programmes-and-initiatives/award-and-development/development-opportunities",
    featured: true,
    trending: true,
    status: "published"
  },
  {
    id: "call-for-applications-undp-african-youth-co-creators-council-2026",
    title: "Call for Applications: UNDP African Youth Co-Creators Council 2026",
    category: "Vacancies",
    company: "UNDP",
    location: "Pan-African (Online / Hybrid)",
    country: "Pan-African",
    remote: "Remote",
    date: "2026-07-08",
    deadline: "2026-10-31",
    image: "img/opp_call-for-applications-undp-afr.jpeg",
    shortDescription: "Applications are now open for the UNDP African Youth Co-Creators Council 2026, an opportunity for young African leaders to help shape the United Natio...",
    description: "Applications are now open for the UNDP African Youth Co-Creators Council 2026, an opportunity for young African leaders to help shape the United Nations Development Program’s (UNDP) youth agenda across the continent. UNDP is seeking 11 exceptional young leaders from Africa&#8217;s five regions and the diaspora to serve on a two-year advisory council that will co-create and influence programmes designed to advance youth development, innovation, and inclusive growth across Africa. Council members will work alongside UNDP teams in Addis Ababa, Dakar, Nairobi, and Pretoria while engaging with Country Offices in 46 African countries to help design, implement, monitor, and evaluate youth-focused initiatives. Eligibility Criteria Applicants must: Be an African national aged 18–34 years at the time of selection. Live in Africa or be part of the African diaspora (a maximum of two positions are reserved for diaspora applicants). Demonstrate leadership, innovation, or community engagement experience in areas such as: Systems thinking Communication Governance Political transitions Peacebuilding Climate action Mental health Gender equality Green, blue, or orange economy Poverty reduction Digital innovation Be committed to inclusive development and youth empowerment. Have not previously served on a UN or development partner Youth Advisory Council, Youth Sounding Board, or similar advisory mechanism. Be available for regular virtual meetings and periodic in-person engagements. Have strong written and spoken English. Be able to serve in a personal and non-political capacity. Applicants may apply as individuals or members of youth organisations, although selected members will serve in their personal capacity. UNDP will prioritize gender balance, regional representation, linguistic diversity, persons with disabilities, and young people from rural and underserved communities.Advertisement (adsbygoogle = window.adsbygoogle || []).push({}); What Council Members Will Do The Council&#8217;s work is built around three core pillars: Advise: Contribute to the design, implementation, monitoring, and evaluation of UNDP projects and flagship initiatives. Anticipate: Identify emerging trends, opportunities, and challenges affecting young Africans and recommend innovative policy and programme responses. Amplify: Strengthen leadership skills through capacity-building while driving positive change within their communities. &nbsp; Why You Should Apply Selected members will have the opportunity to: Influence UNDP&#8217;s Africa Youth Portfolio and regional programmes. Work directly with UNDP teams across Africa. Strengthen leadership and policy engagement skills. Collaborate with young leaders from across Africa and the diaspora. Contribute to shaping policies and programmes that impact millions of young Africans. Selection Process Applications will undergo: Eligibility screening by UNDP Country Offices. Shortlisting by the Regional Service Centre for Africa (RSCA). Interviews conducted by a panel comprising UNDP staff, Country Offices, and development partners. Final selection based on merit, diversity, and inclusion criteria. How to Apply Interested applicants should submit their applications through the official UNDP application page: https://www.undp.org/africa/blog/call-applications-african-youth-co-creators-council Application Deadline: Not specified. &nbsp;Advertisement (adsbygoogle = window.adsbygoogle || []).push({}); ShareRelated Posts:MSME Africa Unveils Top 50 Remarkable MSME Founders&hellip;Ultimate 2026 Guide to the Top Business Enabling&hellip;Overcoming Binding Constraints to Competitive&hellip;Call For Applications: African Young Women Leaders&hellip;15 Social Media Management Tools That Will Make Life&hellip;African Women Impact Fund launches with USD$60&hellip; Related Topics:Advisory CouncilAfricaAfrican YouthClimate ActionDigital InnovationFellowshipGovernanceInnovationLeadership OpportunityPeaceBuildingUNDPyouth developmentYouth Leadership Up Next Call For Applications: AGFUND Development Programme 2026 for Diana Award Recipients in Africa\n\nCall for Applications: African Fashion FundLab Fellowship 2026 | Fully Funded Business Development Program for Fashion Entrepreneurs",
    requirements: ["Be an African national aged 18–34 years at the time of selection.","Live in Africa or be part of the African diaspora (a maximum of two positions are reserved for diaspora applicants).","Demonstrate leadership, innovation, or community engagement experience in areas such as: Systems thinking","Communication"],
    benefits: ["African Women Impact Fund launches with USD$60&hellip;","Access to world-class mentorship and business coaching.","Opportunity to network with peers and global industry experts.","Financial support, stipend, or equity-free grants (where applicable)."],
    skills: ["Leadership","Problem Solving"],
    experienceLevel: "Graduate",
    applyUrl: "https://www.undp.org/africa/blog/call-applications-african-youth-co-creators-council",
    featured: true,
    trending: true,
    status: "published"
  },
  {
    id: "call-for-applications-african-fashion-fundlab-fellowship-2026-fully-funded-business-development-program-for-fashion-entrepreneurs",
    title: "Call for Applications: African Fashion FundLab Fellowship 2026 | Fully Funded Business Development Program for Fashion Entrepreneurs",
    category: "Fellowships",
    company: "African Fashion FundLab",
    location: "Nigeria (Online / Remote)",
    country: "Nigeria",
    remote: "Remote",
    date: "2026-07-08",
    deadline: "2026-10-31",
    image: "img/opp_call-for-applications-african-.jpeg",
    shortDescription: "Applications are now open for the African Fashion FundLab Fellowship 2026, a fully funded business development programme designed to help fashion entr...",
    description: "Applications are now open for the African Fashion FundLab Fellowship 2026, a fully funded business development programme designed to help fashion entrepreneurs in Nigeria build sustainable, investment-ready businesses. The fellowship is specifically designed for established fashion business owners seeking to strengthen their operations, improve financial management, attract investment, and scale their brands through practical, hands-on learning and expert mentorship. &nbsp; Eligibility Criteria Applicants must: Own a registered fashion business Have operated their business for at least three (3) years Be available to participate in the fellowship in person Why You Should Apply The African Fashion FundLab Fellowship offers fashion entrepreneurs the opportunity to strengthen their businesses through practical learning, expert guidance, and investment readiness support. Participants will leave the programme with the knowledge, tools, and network needed to build profitable and sustainable fashion brands. &nbsp;Advertisement (adsbygoogle = window.adsbygoogle || []).push({}); What You&#8217;ll Learn Participants will gain practical skills to: Build and scale a sustainable fashion business Develop profitable business models and increase revenue Position their fashion brands to attract investors and secure funding Strengthen bookkeeping and financial management systems Understand legal, compliance, and regulatory requirements Improve operational efficiency for long-term business growth Programme Benefits Successful applicants will receive: Fully funded participation at no cost Expert-led business development training One-on-one mentorship from experienced industry professionals Investment readiness support Networking opportunities with fellow fashion entrepreneurs Certificate of participation upon successful completion of the fellowship &nbsp; How to Apply Interested applicants should complete the online application form: https://docs.google.com/forms/d/e/1FAIpQLScs41USLSx5ersIcpvSUkTjXmaComOnVRmEaHYCRzJa72e6sw/viewform Application Deadline: Not specified. &nbsp;Advertisement (adsbygoogle = window.adsbygoogle || []).push({}); ShareRelated Posts:MSME Africa Unveils Top 50 Remarkable MSME Founders&hellip;Ultimate 2026 Guide to the Top Business Enabling&hellip;15 Social Media Management Tools That Will Make Life&hellip;Overcoming Binding Constraints to Competitive&hellip;15+ Bank Initiatives, Programs and Products for&hellip;How African Startups Can Secure Funding in Tough Markets Related Topics:African Fashion FundLabBusiness DevelopmentEntrepreneurshipFashion BusinessFashion EntrepreneursFashion FellowshipFully FundedInvestment ReadinessMentorshipNigeria Up Next Call for Applications: UNDP African Youth Co-Creators Council 2026\n\nUBA Announces Tony Elumelu’s Retirement, Appoints Emmanuel Nnorom as Chairman",
    requirements: ["Own a registered fashion business","Have operated their business for at least three (3) years","Be available to participate in the fellowship in person","Build and scale a sustainable fashion business"],
    benefits: ["Position their fashion brands to attract investors and secure funding","Fully funded participation at no cost","One-on-one mentorship from experienced industry professionals","How African Startups Can Secure Funding in Tough Markets"],
    skills: ["Leadership","Problem Solving"],
    experienceLevel: "Graduate",
    applyUrl: "https://docs.google.com/forms/d/e/1FAIpQLScs41USLSx5ersIcpvSUkTjXmaComOnVRmEaHYCRzJa72e6sw/viewform",
    featured: true,
    trending: false,
    status: "published"
  },
  {
    id: "call-for-applications-dangote-cement-undergraduate-engineering-research-competition-2026-up-to-15-million",
    title: "Call for Applications: Dangote Cement Undergraduate Engineering Research Competition 2026 (Up to ₦1.5 Million)",
    category: "Vacancies",
    company: "Dangote Cement",
    location: "Kenya (Hybrid / Onsite)",
    country: "Kenya",
    remote: "Onsite",
    date: "2026-07-08",
    deadline: "2026-10-31",
    image: "img/opp_call-for-applications-dangote-.png",
    shortDescription: "Call For Applications: Standard Chartered Foundation Women in Tech Accelerator Kenya (Cohort 9) – Up to $10,000 Equity-Free Grant...",
    description: "Call For Applications: Standard Chartered Foundation Women in Tech Accelerator Kenya (Cohort 9) – Up to $10,000 Equity-Free Grant",
    requirements: ["Be currently enrolled undergraduate engineering students in Nigeria","Apply as a team of 2–4 students","Belong to the same university","Submit original and unpublished work"],
    benefits: ["Professional mentorship from industry experts","International Breweries’ Kickstart Initiative Awards&hellip;"],
    skills: ["Leadership","Problem Solving"],
    experienceLevel: "Undergraduate",
    applyUrl: "https://docs.google.com/forms/d/e/1FAIpQLSfugF-KnVn7qHRuXgsXpf1axFZ7iNAwto0LgwZRbxKaqjvjIg/viewform",
    featured: true,
    trending: false,
    status: "published"
  },
];

const DEFAULT_CATEGORIES = [
  { name: "Jobs", icon: "briefcase", count: 18, description: "Full-time and part-time careers at leading companies" },
  { name: "Grants", icon: "gift", count: 10, description: "Non-dilutive financial awards for startups and academic research" },
  { name: "Scholarships", icon: "graduation-cap", count: 8, description: "Fully and partially funded academic scholarships globally" },
  { name: "Internships", icon: "user-clock", count: 20, description: "Graduate and undergraduate industrial training roles" },
  { name: "Business Funding", icon: "hand-holding-usd", count: 6, description: "Equity investments and accelerator seed funding" },
  { name: "Vacancies", icon: "clipboard-list", count: 3, description: "Contract roles, consultancy listings, and urgent job openings" },
  { name: "Fellowships", icon: "users", count: 5, description: "Structured development cohorts and open-source programs" }
];

const FAQS = [
  {
    question: "Do I need to sign up or register to use Afri Tech Hub?",
    answer: "No, Afri Tech Hub is an open-access platform. You do not need to create an account, register, or provide any personal details to browse, search, and apply for opportunities."
  },
  {
    question: "How do I apply for an opportunity?",
    answer: "Each opportunity post has a 'Read More' button that opens the details page. There you will find the application requirements, benefits, and a direct 'Apply Now' button that will take you to the official application portal of the provider."
  },
  {
    question: "Are the opportunities on this site verified?",
    answer: "Yes, our team curates and verifies each opportunity from official, reputable sources (e.g., multinational companies, top universities, global philanthropies, and government bodies) before listing them."
  },
  {
    question: "How can I post an opportunity on Afri Tech Hub?",
    answer: "Only authenticated administrators can create, edit, or delete opportunities via the Admin Dashboard. If you have an external vacancy you'd like to share, you can reach out to our team via the Contact Page, and we will verify and list it for you."
  },
  {
    question: "What is the Afri Tech Hub WhatsApp Community?",
    answer: "It is our instant notification network on WhatsApp where we share urgent deadlines, daily vacancies, and grant alerts directly to your phone. You can join it using the floating WhatsApp button at the bottom-right corner of the website."
  }
];

const CATEGORY_IMAGES = {
  "jobs": "img/unsplash_photo-1522071820081-009f0129c71c.jpg",
  "grants": "img/unsplash_photo-1454165804606-c3d57bc86b40.jpg",
  "scholarships": "img/unsplash_photo-1523240795612-9a054b0db644.jpg",
  "internships": "img/unsplash_photo-1517245386807-bb43f82c33c4.jpg",
  "business funding": "img/unsplash_photo-1559526324-4b87b5e36e44.jpg",
  "vacancies": "img/unsplash_photo-1486406146926-c627a92ad1ab.jpg",
  "fellowships": "img/unsplash_photo-1521737604893-d14cc237f11d.jpg"
};

// Global Production Site Configuration for SEO & Canonical Links
const SITE_CONFIG = {
  domain: "https://afritechhub.xyz",
  siteName: "Afri Tech Hub",
  defaultTitle: "Afri Tech Hub | Opportunities for African Talent",
  defaultDescription: "Discover jobs, grants, fellowships, scholarships, internships and other opportunities for African talent on Afri Tech Hub.",
  defaultImage: "img/logo.png",
  twitterHandle: "@afritechhub",
  contactEmail: "hubafritech@gmail.com",
  contactPhone: "09159701354",
  address: "Aba, Abia State, Nigeria"
};

// Helper functions for data management
const DataStore = {
  SITE_CONFIG,

  // URL-friendly slug generator
  slugify(text) {
    if (!text) return 'opportunity-' + Date.now().toString(36);
    return text
      .toString()
      .toLowerCase()
      .trim()
      .replace(/[^\w\s-]/g, '')     // Remove non-word chars (except spaces & dashes)
      .replace(/[\s_-]+/g, '-')     // Replace spaces, underscores, multiple dashes with single dash
      .replace(/^-+|-+$/g, '');     // Trim leading/trailing dashes
  },

  // Ensure unique slug collision prevention
  generateUniqueSlug(title, currentId = null) {
    const baseSlug = this.slugify(title);
    const opps = this.getOpportunities(true);
    let candidate = baseSlug;
    let counter = 2;

    while (opps.some(o => o.id === candidate && o.id !== currentId)) {
      candidate = `${baseSlug}-${counter}`;
      counter++;
    }
    return candidate;
  },

  // Slug Redirects Management (handles old URL -> new URL redirects)
  getSlugRedirects() {
    const local = localStorage.getItem('ath_slug_redirects');
    if (!local) return {};
    try {
      return JSON.parse(local) || {};
    } catch (e) {
      return {};
    }
  },

  addSlugRedirect(oldSlug, newSlug) {
    if (!oldSlug || !newSlug || oldSlug === newSlug) return;
    const redirects = this.getSlugRedirects();
    redirects[oldSlug] = newSlug;
    localStorage.setItem('ath_slug_redirects', JSON.stringify(redirects));
  },

  getRedirect(slug) {
    const redirects = this.getSlugRedirects();
    let current = slug;
    let depth = 0;
    // Follow redirect chains up to 5 hops
    while (redirects[current] && depth < 5) {
      current = redirects[current];
      depth++;
    }
    return current !== slug ? current : null;
  },

  // Automatically Generate Opportunity SEO Metadata
  getSEOMetadata(opp) {
    if (!opp) return null;
    const cleanTitle = (opp.seoTitle || opp.title || '').trim();
    const seoTitle = cleanTitle.toLowerCase().includes('afri tech hub') 
      ? cleanTitle 
      : `${cleanTitle} | Afri Tech Hub`;

    let metaDesc = (opp.seoDescription || '').trim();
    if (!metaDesc) {
      const summaryText = opp.shortDescription || opp.description || '';
      const cleanSummary = summaryText.replace(/<[^>]*>?/gm, '').replace(/\s+/g, ' ').trim();
      const companyPart = opp.company ? ` at ${opp.company}` : '';
      const locationPart = opp.location ? ` in ${opp.location}` : '';
      const deadlinePart = opp.deadline ? ` Apply before ${opp.deadline}.` : '';
      const prefix = `Apply for ${opp.title}${companyPart}${locationPart}.${deadlinePart} `;
      const combined = (prefix + cleanSummary).slice(0, 155).trim() + '...';
      metaDesc = combined;
    }

    const canonicalUrl = `${SITE_CONFIG.domain}/opportunities/${encodeURIComponent(opp.id)}`;
    const imageUrl = opp.image 
      ? (opp.image.startsWith('http') ? opp.image : `${SITE_CONFIG.domain}/${opp.image.replace(/^\//, '')}`)
      : `${SITE_CONFIG.domain}/img/logo.png`;

    return {
      title: seoTitle,
      description: metaDesc,
      canonical: canonicalUrl,
      url: canonicalUrl,
      image: imageUrl,
      category: opp.category || 'Opportunities',
      company: opp.company || 'Afri Tech Hub',
      publishedTime: opp.date ? new Date(opp.date).toISOString() : new Date().toISOString(),
      structuredData: this.generateStructuredData(opp, canonicalUrl, imageUrl)
    };
  },

  // Accurate Schema.org JSON-LD Structured Data
  generateStructuredData(opp, canonicalUrl, imageUrl) {
    const cat = (opp.category || '').toLowerCase();
    const url = canonicalUrl || `${SITE_CONFIG.domain}/opportunities/${encodeURIComponent(opp.id)}`;
    const img = imageUrl || `${SITE_CONFIG.domain}/img/logo.png`;
    const cleanDesc = (opp.description || opp.shortDescription || opp.title || '')
      .replace(/<[^>]*>?/gm, '')
      .replace(/\s+/g, ' ')
      .trim();

    // 1. Genuine Employment / Vacancies / Internships -> JobPosting
    if (cat === 'jobs' || cat === 'vacancies' || cat === 'internships') {
      const validThrough = (opp.deadline && opp.deadline !== 'Rolling' && !isNaN(Date.parse(opp.deadline)))
        ? new Date(opp.deadline).toISOString()
        : undefined;

      const jobPosting = {
        "@context": "https://schema.org",
        "@type": "JobPosting",
        "title": opp.title,
        "description": cleanDesc,
        "datePosted": opp.date || new Date().toISOString().split('T')[0],
        "employmentType": cat === 'internships' ? "INTERN" : (opp.remote === 'Remote' ? "FULL_TIME" : "FULL_TIME"),
        "hiringOrganization": {
          "@type": "Organization",
          "name": opp.company || "Afri Tech Hub",
          "logo": img
        },
        "jobLocation": {
          "@type": "Place",
          "address": {
            "@type": "PostalAddress",
            "addressCountry": opp.country || "Africa",
            "addressLocality": opp.location || "Africa"
          }
        },
        "url": url,
        "directApply": true
      };

      if (opp.remote === 'Remote') {
        jobPosting.jobLocationType = "TELECOMMUTE";
        jobPosting.applicantLocationRequirements = {
          "@type": "Country",
          "name": opp.country || "Africa"
        };
      }
      if (validThrough) {
        jobPosting.validThrough = validThrough;
      }
      return jobPosting;
    }

    // 2. Grants & Business Funding -> MonetaryGrant
    if (cat === 'grants' || cat === 'business funding' || cat === 'funding') {
      return {
        "@context": "https://schema.org",
        "@type": "MonetaryGrant",
        "name": opp.title,
        "description": cleanDesc,
        "funder": {
          "@type": "Organization",
          "name": opp.company || "Afri Tech Hub",
          "logo": img
        },
        "url": url
      };
    }

    // 3. Scholarships & Fellowships -> EducationalOccupationalCredential
    if (cat === 'scholarships' || cat === 'fellowships') {
      return {
        "@context": "https://schema.org",
        "@type": "EducationalOccupationalCredential",
        "name": opp.title,
        "description": cleanDesc,
        "credentialCategory": opp.category,
        "recognizedBy": {
          "@type": "Organization",
          "name": opp.company || "Afri Tech Hub",
          "logo": img
        },
        "url": url
      };
    }

    // 4. Default compliant WebPage / Article structured data
    return {
      "@context": "https://schema.org",
      "@type": "Article",
      "headline": opp.title,
      "description": cleanDesc,
      "image": img,
      "datePublished": opp.date ? new Date(opp.date).toISOString() : new Date().toISOString(),
      "author": {
        "@type": "Organization",
        "name": opp.company || "Afri Tech Hub"
      },
      "publisher": {
        "@type": "Organization",
        "name": "Afri Tech Hub",
        "logo": {
          "@type": "ImageObject",
          "url": `${SITE_CONFIG.domain}/img/logo.png`
        }
      },
      "mainEntityOfPage": {
        "@type": "WebPage",
        "@id": url
      }
    };
  },

  // Global Schema.org definitions for Organization and WebSite
  getGlobalSchemas() {
    return [
      {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "Afri Tech Hub",
        "url": `${SITE_CONFIG.domain}/`,
        "description": SITE_CONFIG.defaultDescription,
        "potentialAction": {
          "@type": "SearchAction",
          "target": `${SITE_CONFIG.domain}/#opportunities?search={search_term_string}`,
          "query-input": "required name=search_term_string"
        }
      },
      {
        "@context": "https://schema.org",
        "@type": "Organization",
        "name": "Afri Tech Hub",
        "url": `${SITE_CONFIG.domain}/`,
        "logo": `${SITE_CONFIG.domain}/img/logo.png`,
        "email": SITE_CONFIG.contactEmail,
        "telephone": `+234${SITE_CONFIG.contactPhone.replace(/^0/, '')}`,
        "address": {
          "@type": "PostalAddress",
          "addressLocality": "Aba",
          "addressRegion": "Abia State",
          "addressCountry": "Nigeria"
        },
        "sameAs": [
          "https://twitter.com/afritechhub",
          "https://linkedin.com/company/afritechhub",
          "https://instagram.com/afritechhub"
        ]
      }
    ];
  },

  // Dynamic Sitemap.xml Generator
  generateSitemapXml(domain = SITE_CONFIG.domain) {
    const base = domain.replace(/\/+$/, '');
    const now = new Date().toISOString().split('T')[0];

    // Static core public pages
    const staticPages = [
      { path: "/", priority: "1.0", changefreq: "daily", lastmod: now },
      { path: "/opportunities/", priority: "0.9", changefreq: "daily", lastmod: now },
      { path: "/about", priority: "0.7", changefreq: "monthly", lastmod: "2026-07-01" },
      { path: "/faq", priority: "0.6", changefreq: "monthly", lastmod: "2026-07-01" },
      { path: "/contact", priority: "0.6", changefreq: "monthly", lastmod: "2026-07-01" },
      { path: "/privacy", priority: "0.4", changefreq: "yearly", lastmod: "2026-07-04" },
      { path: "/terms", priority: "0.4", changefreq: "yearly", lastmod: "2026-06-28" }
    ];

    // Published opportunities ONLY (strict: excludes drafts, deleted, admin)
    const publishedOpps = this.getOpportunities(false);
    const opportunityPages = publishedOpps.map(opp => ({
      loc: `${base}/opportunities/${encodeURIComponent(opp.id)}`,
      lastmod: opp.date && !isNaN(Date.parse(opp.date)) ? new Date(opp.date).toISOString().split('T')[0] : now,
      changefreq: "weekly",
      priority: opp.featured ? "0.9" : "0.8"
    }));

    let xml = `<?xml version="1.0" encoding="UTF-8"?>\n`;
    xml += `<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n`;

    staticPages.forEach(p => {
      xml += `  <url>\n`;
      xml += `    <loc>${base}${p.path}</loc>\n`;
      xml += `    <lastmod>${p.lastmod}</lastmod>\n`;
      xml += `    <changefreq>${p.changefreq}</changefreq>\n`;
      xml += `    <priority>${p.priority}</priority>\n`;
      xml += `  </url>\n`;
    });

    opportunityPages.forEach(p => {
      xml += `  <url>\n`;
      xml += `    <loc>${p.loc}</loc>\n`;
      xml += `    <lastmod>${p.lastmod}</lastmod>\n`;
      xml += `    <changefreq>${p.changefreq}</changefreq>\n`;
      xml += `    <priority>${p.priority}</priority>\n`;
      xml += `  </url>\n`;
    });

    xml += `</urlset>\n`;
    return xml;
  },

  // Get all opportunities (including drafts/archived for admin, filtered for public)
  getOpportunities(includeAdminAll = false) {
    let list = [];
    const local = localStorage.getItem('ath_opportunities');
    if (local) {
      try {
        list = JSON.parse(local);
      } catch (e) {
        console.error("Failed to parse local storage opportunities, resetting...", e);
        list = DEFAULT_OPPORTUNITIES;
        localStorage.setItem('ath_opportunities', JSON.stringify(list));
      }
    } else {
      list = DEFAULT_OPPORTUNITIES;
      localStorage.setItem('ath_opportunities', JSON.stringify(list));
    }
    
    if (includeAdminAll) {
      return list;
    } else {
      // Public only sees 'published' status
      return list.filter(opp => opp.status === 'published' || !opp.status);
    }
  },

  // Save new or update existing opportunity
  saveOpportunity(opp, previousSlug = null) {
    // Assign image fallback if missing
    if (!opp.image || opp.image.trim() === '') {
      const catKey = opp.category ? opp.category.toLowerCase() : "jobs";
      opp.image = CATEGORY_IMAGES[catKey] || CATEGORY_IMAGES["jobs"];
    }

    // Default status to 'published' if not provided
    opp.status = opp.status || 'published';

    // Ensure slug is clean and valid
    if (!opp.id || opp.id.trim() === '') {
      opp.id = this.generateUniqueSlug(opp.title);
    } else {
      opp.id = this.slugify(opp.id);
    }

    // Automatically generate SEO title & description if not provided
    if (!opp.seoTitle || opp.seoTitle.trim() === '') {
      opp.seoTitle = `${opp.title} | Afri Tech Hub`;
    }
    if (!opp.seoDescription || opp.seoDescription.trim() === '') {
      const descSnippet = (opp.description || opp.shortDescription || '').replace(/<[^>]*>?/gm, '').replace(/\s+/g, ' ').slice(0, 155).trim();
      opp.seoDescription = descSnippet ? `${descSnippet}...` : `Apply for ${opp.title} at ${opp.company || 'Afri Tech Hub'}.`;
    }

    // Check for slug redirection if editing
    if (previousSlug && previousSlug !== opp.id) {
      this.addSlugRedirect(previousSlug, opp.id);
    }

    const list = this.getOpportunities(true);
    const existingIndex = list.findIndex(o => o.id === opp.id || (previousSlug && o.id === previousSlug));
    
    if (existingIndex > -1) {
      list[existingIndex] = opp; // Update existing record
    } else {
      list.unshift(opp); // Insert new record at top
    }

    localStorage.setItem('ath_opportunities', JSON.stringify(list));

    // Cache updated dynamic sitemap in localStorage for admin export
    try {
      localStorage.setItem('ath_cached_sitemap', this.generateSitemapXml());
    } catch (e) {
      // ignore
    }

    return list;
  },

  // Delete opportunity
  deleteOpportunity(oppId) {
    const list = this.getOpportunities(true);
    const filtered = list.filter(o => o.id !== oppId);
    localStorage.setItem('ath_opportunities', JSON.stringify(filtered));

    // Cache updated dynamic sitemap in localStorage
    try {
      localStorage.setItem('ath_cached_sitemap', this.generateSitemapXml());
    } catch (e) {
      // ignore
    }

    return filtered;
  },

  // Get related opportunities based on category or skills matching
  getRelatedOpportunities(currentPostId, limit = 3) {
    const all = this.getOpportunities();
    const current = all.find(o => o.id === currentPostId);
    if (!current) return all.filter(o => o.id !== currentPostId).slice(0, limit);

    const candidates = all.filter(o => o.id !== currentPostId);

    const scored = candidates.map(opp => {
      let score = 0;
      if (opp.category && current.category && opp.category.toLowerCase() === current.category.toLowerCase()) {
        score += 5;
      }
      if (opp.company && current.company && opp.company.toLowerCase() === current.company.toLowerCase()) {
        score += 3;
      }
      if (opp.skills && current.skills) {
        const currentSkillsLower = current.skills.map(s => s.toLowerCase());
        const matches = opp.skills.filter(s => currentSkillsLower.includes(s.toLowerCase()));
        score += matches.length * 2;
      }
      return { opp, score };
    });

    scored.sort((a, b) => b.score - a.score);
    return scored.slice(0, limit).map(item => item.opp);
  },

  // Categories list management
  getCategories() {
    const local = localStorage.getItem('ath_categories');
    let cats = [];
    if (local) {
      try {
        cats = JSON.parse(local);
      } catch (e) {
        cats = DEFAULT_CATEGORIES;
      }
    } else {
      cats = DEFAULT_CATEGORIES;
      localStorage.setItem('ath_categories', JSON.stringify(cats));
    }

    // Dynamic counts based on ALL (admin) opportunities
    const allOpps = this.getOpportunities(true);
    return cats.map(cat => {
      const count = allOpps.filter(o => o.category.toLowerCase() === cat.name.toLowerCase() && o.status === 'published').length;
      return { ...cat, count };
    });
  },

  saveCategory(cat) {
    const local = localStorage.getItem('ath_categories');
    let cats = local ? JSON.parse(local) : DEFAULT_CATEGORIES;
    
    const existingIndex = cats.findIndex(c => c.name.toLowerCase() === cat.name.toLowerCase());
    if (existingIndex > -1) {
      cats[existingIndex] = cat;
    } else {
      cats.push(cat);
    }
    localStorage.setItem('ath_categories', JSON.stringify(cats));
    return cats;
  },

  deleteCategory(catName) {
    const local = localStorage.getItem('ath_categories');
    let cats = local ? JSON.parse(local) : DEFAULT_CATEGORIES;
    const filtered = cats.filter(c => c.name.toLowerCase() !== catName.toLowerCase());
    localStorage.setItem('ath_categories', JSON.stringify(filtered));
    return filtered;
  },

  // Newsletter Signups & Subscriber Management
  getSubscribers() {
    const local = localStorage.getItem('ath_subscribers');
    if (!local) return [];
    try {
      const parsed = JSON.parse(local);
      if (!Array.isArray(parsed)) return [];
      // Migrate legacy string array to objects seamlessly
      const migrated = parsed.map((item, idx) => {
        if (typeof item === 'string') {
          return {
            id: 'sub-' + (Date.now() - (idx * 1000)),
            email: item,
            subscribedAt: new Date(Date.now() - (idx * 86400000)).toISOString(),
            status: 'active',
            unsubscribedAt: null,
            source: 'Homepage'
          };
        }
        return item;
      });
      // Save migrated structure if any migration occurred
      if (parsed.some(item => typeof item === 'string')) {
        localStorage.setItem('ath_subscribers', JSON.stringify(migrated));
      }
      return migrated;
    } catch (e) {
      console.error("Failed to parse subscribers, resetting...", e);
      return [];
    }
  },

  addSubscriber(email, source = 'Homepage') {
    const cleanEmail = (email || '').trim().toLowerCase();
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(cleanEmail)) {
      return { success: false, duplicate: false, message: "Please enter a valid email address." };
    }

    const subs = this.getSubscribers();
    const existing = subs.find(s => s.email.toLowerCase() === cleanEmail);
    if (existing) {
      if (existing.status === 'unsubscribed') {
        // Reactivate subscriber
        existing.status = 'active';
        existing.unsubscribedAt = null;
        existing.subscribedAt = new Date().toISOString();
        existing.source = source || existing.source;
        localStorage.setItem('ath_subscribers', JSON.stringify(subs));
        this.syncWithNewsletterProvider('subscribe', existing);
        return { success: true, duplicate: false, reactivated: true, subscriber: existing, message: "You’re resubscribed! Watch your inbox for updates from Afri Tech Hub." };
      }
      return { success: false, duplicate: true, message: "You’re already subscribed to the Afri Tech Hub newsletter." };
    }

    const newSub = {
      id: 'sub-' + Date.now() + '-' + Math.random().toString(36).substring(2, 6),
      email: cleanEmail,
      subscribedAt: new Date().toISOString(),
      status: 'active',
      unsubscribedAt: null,
      source: source || 'Homepage'
    };

    subs.unshift(newSub);
    localStorage.setItem('ath_subscribers', JSON.stringify(subs));
    
    // Asynchronously trigger provider sync
    this.syncWithNewsletterProvider('subscribe', newSub);

    return { success: true, duplicate: false, subscriber: newSub, message: "You’re subscribed! Watch your inbox for updates from Afri Tech Hub." };
  },

  updateSubscriberStatus(id, newStatus) {
    const subs = this.getSubscribers();
    const sub = subs.find(s => s.id === id || s.email.toLowerCase() === id.toLowerCase());
    if (sub) {
      sub.status = newStatus;
      if (newStatus === 'unsubscribed') {
        sub.unsubscribedAt = new Date().toISOString();
      } else {
        sub.unsubscribedAt = null;
      }
      localStorage.setItem('ath_subscribers', JSON.stringify(subs));
      this.syncWithNewsletterProvider(newStatus === 'active' ? 'subscribe' : 'unsubscribe', sub);
    }
    return subs;
  },

  deleteSubscriber(idOrEmail) {
    const subs = this.getSubscribers();
    const filtered = subs.filter(s => s.id !== idOrEmail && s.email.toLowerCase() !== idOrEmail.toLowerCase());
    localStorage.setItem('ath_subscribers', JSON.stringify(filtered));
    return filtered;
  },

  getSubscriberStats() {
    const subs = this.getSubscribers();
    const now = new Date();
    const currentMonth = now.getMonth();
    const currentYear = now.getFullYear();

    const total = subs.length;
    const active = subs.filter(s => s.status === 'active').length;
    const unsubscribed = subs.filter(s => s.status === 'unsubscribed').length;
    const newThisMonth = subs.filter(s => {
      if (!s.subscribedAt) return false;
      const d = new Date(s.subscribedAt);
      return d.getMonth() === currentMonth && d.getFullYear() === currentYear;
    }).length;

    return { total, active, unsubscribed, newThisMonth };
  },

  exportSubscribersCSV(filterStatus = 'all') {
    let subs = this.getSubscribers();
    if (filterStatus !== 'all') {
      subs = subs.filter(s => s.status === filterStatus);
    }

    const headers = ["Subscriber ID", "Email Address", "Date Subscribed", "Status", "Unsubscribed At", "Source"];
    const rows = subs.map(s => [
      `"${s.id || ''}"`,
      `"${s.email || ''}"`,
      `"${s.subscribedAt ? new Date(s.subscribedAt).toLocaleString() : ''}"`,
      `"${s.status || 'active'}"`,
      `"${s.unsubscribedAt ? new Date(s.unsubscribedAt).toLocaleString() : ''}"`,
      `"${s.source || 'Homepage'}"`
    ]);

    return [headers.join(','), ...rows.map(r => r.join(','))].join('\n');
  },

  // Newsletter Email Marketing Provider Service Abstraction
  async syncWithNewsletterProvider(action, subscriber) {
    const provider = window.NEWSLETTER_PROVIDER || 'brevo';
    const apiKey = window.NEWSLETTER_API_KEY || null;
    const listId = window.NEWSLETTER_LIST_ID || null;

    console.log(`[NewsletterProvider:${provider}] Syncing action: ${action}`, subscriber);

    try {
      if (provider === 'webhook' && window.NEWSLETTER_WEBHOOK_URL) {
        await fetch(window.NEWSLETTER_WEBHOOK_URL, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ action, subscriber, apiKey, listId })
        });
      }
      return { success: true, provider };
    } catch (err) {
      console.warn(`[NewsletterProvider:${provider}] Sync warning:`, err);
      return { success: false, error: err.message };
    }
  },

  async sendNewsletter({ subject, content, audience = 'active' }) {
    const subs = this.getSubscribers();
    const recipients = subs.filter(s => audience === 'all' || s.status === 'active');
    
    if (recipients.length === 0) {
      return { success: false, message: "No active subscribers found in selected audience." };
    }

    console.log(`[SendNewsletter] Sending "${subject}" to ${recipients.length} recipients.`);
    await new Promise(resolve => setTimeout(resolve, 800));

    return {
      success: true,
      recipientCount: recipients.length,
      message: `Newsletter successfully sent to ${recipients.length} subscriber(s).`
    };
  },

  // Contact Support Messages
  getContactMessages() {
    const local = localStorage.getItem('ath_contact_messages');
    return local ? JSON.parse(local) : [];
  },

  addContactMessage(msg) {
    const msgs = this.getContactMessages();
    msg.id = 'msg-' + Date.now() + '-' + Math.random().toString(36).substr(2, 4);
    msg.date = new Date().toISOString();
    msg.replied = false;
    msgs.unshift(msg);
    localStorage.setItem('ath_contact_messages', JSON.stringify(msgs));
    return msgs;
  },

  toggleReplyMessage(msgId) {
    const msgs = this.getContactMessages();
    const existing = msgs.find(m => m.id === msgId);
    if (existing) {
      existing.replied = !existing.replied;
      localStorage.setItem('ath_contact_messages', JSON.stringify(msgs));
    }
    return msgs;
  },

  deleteMessage(msgId) {
    const msgs = this.getContactMessages();
    const filtered = msgs.filter(m => m.id !== msgId);
    localStorage.setItem('ath_contact_messages', JSON.stringify(filtered));
    return filtered;
  },

  getFaqs() {
    return FAQS;
  },

  // =========================================================================
  // FIRST-PARTY WEBSITE ANALYTICS DATA LAYER
  // =========================================================================

  /**
   * Record a new analytics event to local storage
   */
  addAnalyticsEvent(event) {
    if (typeof localStorage === 'undefined') return event;

    try {
      this.seedInitialAnalyticsIfEmpty();

      const local = localStorage.getItem('ath_analytics_events');
      let events = local ? JSON.parse(local) : [];

      // Validate & normalize
      const sanitized = {
        id: event.id || 'evt_' + Date.now().toString(36) + '_' + Math.random().toString(36).substr(2, 6),
        event_type: event.event_type || 'page_view',
        opportunity_id: event.opportunity_id || null,
        opportunity_title: event.opportunity_title || null,
        page_path: event.page_path || '/',
        session_id: event.session_id || 'sess_unknown',
        visitor_id: event.visitor_id || 'vis_unknown',
        country: event.country || 'Unknown',
        country_code: event.country_code || 'XX',
        referrer: event.referrer || 'Direct',
        device_type: event.device_type || 'Desktop',
        created_at: event.created_at || new Date().toISOString()
      };

      events.push(sanitized);

      // Enforce storage cap (keep last 8,000 events)
      if (events.length > 8000) {
        events = events.slice(-8000);
      }

      try {
        localStorage.setItem('ath_analytics_events', JSON.stringify(events));
      } catch (quotaErr) {
        // In case storage quota is reached, retain latest 3,000 events
        events = events.slice(-3000);
        localStorage.setItem('ath_analytics_events', JSON.stringify(events));
      }

      return sanitized;
    } catch (err) {
      console.warn('DataStore.addAnalyticsEvent error:', err);
      return event;
    }
  },

  /**
   * Retrieve analytics events with optional filtering
   */
  getAnalyticsEvents(filter = {}) {
    if (typeof localStorage === 'undefined') return [];

    this.seedInitialAnalyticsIfEmpty();

    try {
      const local = localStorage.getItem('ath_analytics_events');
      let events = local ? JSON.parse(local) : [];

      if (filter.dateFrom) {
        const fromTs = new Date(filter.dateFrom).getTime();
        events = events.filter((e) => new Date(e.created_at).getTime() >= fromTs);
      }

      if (filter.dateTo) {
        const toTs = new Date(filter.dateTo).getTime();
        events = events.filter((e) => new Date(e.created_at).getTime() <= toTs);
      }

      if (filter.eventType) {
        events = events.filter((e) => e.event_type === filter.eventType);
      }

      if (filter.opportunityId) {
        events = events.filter((e) => e.opportunity_id === filter.opportunityId);
      }

      return events;
    } catch (err) {
      console.error('DataStore.getAnalyticsEvents error:', err);
      return [];
    }
  },

  /**
   * Calculate high-level performance overview metrics
   */
  getAnalyticsOverview(dateFrom, dateTo) {
    const events = this.getAnalyticsEvents({ dateFrom, dateTo });

    const visitors = new Set();
    const sessions = new Set();
    let impressions = 0;
    let pageViews = 0;
    let oppViews = 0;
    let oppClicks = 0;
    let applyClicks = 0;

    events.forEach((e) => {
      if (e.visitor_id) visitors.add(e.visitor_id);
      if (e.session_id) sessions.add(e.session_id);

      switch (e.event_type) {
        case 'opportunity_impression':
          impressions++;
          break;
        case 'page_view':
          pageViews++;
          break;
        case 'opportunity_view':
          oppViews++;
          break;
        case 'opportunity_click':
          oppClicks++;
          break;
        case 'apply_click':
          applyClicks++;
          break;
      }
    });

    const ctr = impressions > 0 ? ((oppClicks / impressions) * 100).toFixed(1) : '0.0';
    const applyConversion = oppViews > 0 ? ((applyClicks / oppViews) * 100).toFixed(1) : '0.0';

    return {
      totalEvents: events.length,
      impressions,
      pageViews,
      opportunityViews: oppViews,
      opportunityClicks: oppClicks,
      applyClicks,
      uniqueVisitors: visitors.size,
      uniqueSessions: sessions.size,
      ctr,
      applyConversion
    };
  },

  /**
   * Aggregate metrics grouped per opportunity
   */
  getAnalyticsByOpportunity(dateFrom, dateTo) {
    const events = this.getAnalyticsEvents({ dateFrom, dateTo });
    const allOpps = this.getOpportunities(true);
    const oppMap = {};

    // Initialize all existing opportunities in the catalog
    allOpps.forEach((opp) => {
      oppMap[opp.id] = {
        id: opp.id,
        title: opp.title,
        company: opp.company,
        category: opp.category,
        status: opp.status || 'published',
        impressions: 0,
        views: 0,
        clicks: 0,
        applyClicks: 0
      };
    });

    // Aggregate event counts
    events.forEach((e) => {
      const oppId = e.opportunity_id;
      if (!oppId) return;

      if (!oppMap[oppId]) {
        oppMap[oppId] = {
          id: oppId,
          title: e.opportunity_title || oppId,
          company: 'Unknown',
          category: 'Other',
          status: 'published',
          impressions: 0,
          views: 0,
          clicks: 0,
          applyClicks: 0
        };
      }

      const item = oppMap[oppId];
      if (e.event_type === 'opportunity_impression') item.impressions++;
      else if (e.event_type === 'opportunity_view') item.views++;
      else if (e.event_type === 'opportunity_click') item.clicks++;
      else if (e.event_type === 'apply_click') item.applyClicks++;
    });

    // Calculate CTR and conversion rates
    const list = Object.values(oppMap).map((item) => {
      const ctr = item.impressions > 0 ? ((item.clicks / item.impressions) * 100).toFixed(1) : '0.0';
      const conversion = item.views > 0 ? ((item.applyClicks / item.views) * 100).toFixed(1) : '0.0';
      return {
        ...item,
        ctr,
        conversion
      };
    });

    // Sort descending by interactions
    list.sort((a, b) => (b.views + b.applyClicks + b.impressions) - (a.views + a.applyClicks + a.impressions));
    return list;
  },

  /**
   * Aggregate metrics grouped per geographic country
   */
  getAnalyticsByCountry(dateFrom, dateTo) {
    const events = this.getAnalyticsEvents({ dateFrom, dateTo });
    const countryMap = {};
    let totalVisitorsCount = 0;

    events.forEach((e) => {
      const country = e.country && e.country !== 'Unknown' ? e.country : 'Nigeria';
      const code = e.country_code && e.country_code !== 'XX' ? e.country_code : 'NG';

      if (!countryMap[country]) {
        countryMap[country] = {
          country,
          countryCode: code,
          visitors: new Set(),
          pageViews: 0,
          opportunityViews: 0,
          clicks: 0,
          applyClicks: 0
        };
      }

      const entry = countryMap[country];
      if (e.visitor_id) entry.visitors.add(e.visitor_id);

      if (e.event_type === 'page_view') entry.pageViews++;
      else if (e.event_type === 'opportunity_view') entry.opportunityViews++;
      else if (e.event_type === 'opportunity_click') entry.clicks++;
      else if (e.event_type === 'apply_click') entry.applyClicks++;
    });

    const result = Object.values(countryMap).map((entry) => {
      const vCount = entry.visitors.size || 1;
      totalVisitorsCount += vCount;
      return {
        country: entry.country,
        countryCode: entry.countryCode,
        visitors: vCount,
        pageViews: entry.pageViews,
        opportunityViews: entry.opportunityViews,
        clicks: entry.clicks,
        applyClicks: entry.applyClicks
      };
    });

    // Add percentages
    result.forEach((r) => {
      r.percentage = totalVisitorsCount > 0 ? Math.round((r.visitors / totalVisitorsCount) * 100) : 0;
    });

    result.sort((a, b) => b.visitors - a.visitors);
    return result;
  },

  /**
   * Time series daily aggregation for charts
   */
  getAnalyticsTimeSeries(dateFrom, dateTo) {
    const events = this.getAnalyticsEvents({ dateFrom, dateTo });

    // Determine start & end dates
    const start = dateFrom ? new Date(dateFrom) : new Date(Date.now() - 6 * 24 * 60 * 60 * 1000);
    const end = dateTo ? new Date(dateTo) : new Date();

    start.setHours(0, 0, 0, 0);
    end.setHours(23, 59, 59, 999);

    const buckets = {};
    const cur = new Date(start);

    while (cur <= end) {
      const dateKey = cur.toISOString().split('T')[0];
      const monthNames = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
      const label = `${monthNames[cur.getMonth()]} ${cur.getDate()}`;

      buckets[dateKey] = {
        label,
        pageViews: 0,
        impressions: 0,
        clicks: 0,
        applyClicks: 0
      };

      cur.setDate(cur.getDate() + 1);
    }

    events.forEach((e) => {
      const dateKey = e.created_at ? e.created_at.split('T')[0] : null;
      if (dateKey && buckets[dateKey]) {
        if (e.event_type === 'page_view') buckets[dateKey].pageViews++;
        else if (e.event_type === 'opportunity_impression') buckets[dateKey].impressions++;
        else if (e.event_type === 'opportunity_click') buckets[dateKey].clicks++;
        else if (e.event_type === 'apply_click') buckets[dateKey].applyClicks++;
      }
    });

    const dates = Object.keys(buckets).sort();
    return {
      labels: dates.map((d) => buckets[d].label),
      pageViews: dates.map((d) => buckets[d].pageViews),
      impressions: dates.map((d) => buckets[d].impressions),
      clicks: dates.map((d) => buckets[d].clicks),
      applyClicks: dates.map((d) => buckets[d].applyClicks)
    };
  },

  /**
   * Device type breakdown for doughnut chart
   */
  getAnalyticsDeviceBreakdown(dateFrom, dateTo) {
    const events = this.getAnalyticsEvents({ dateFrom, dateTo });
    const breakdown = { Desktop: 0, Mobile: 0, Tablet: 0 };

    events.forEach((e) => {
      const dev = e.device_type || 'Desktop';
      if (breakdown[dev] !== undefined) {
        breakdown[dev]++;
      } else {
        breakdown.Desktop++;
      }
    });

    const total = breakdown.Desktop + breakdown.Mobile + breakdown.Tablet;
    return {
      counts: breakdown,
      percentages: {
        Desktop: total > 0 ? Math.round((breakdown.Desktop / total) * 100) : 50,
        Mobile: total > 0 ? Math.round((breakdown.Mobile / total) * 100) : 40,
        Tablet: total > 0 ? Math.round((breakdown.Tablet / total) * 100) : 10
      }
    };
  },

  /**
   * Retrieve recent raw event stream
   */
  getRecentAnalyticsActivity(limit = 15) {
    const events = this.getAnalyticsEvents();
    return events.slice(-limit).reverse();
  },

  /**
   * Export all matching analytics records as a downloadable CSV
   */
  exportAnalyticsCsv(dateFrom, dateTo) {
    const events = this.getAnalyticsEvents({ dateFrom, dateTo });
    const headers = [
      'Event ID',
      'Event Type',
      'Opportunity ID',
      'Opportunity Title',
      'Page Path',
      'Session ID',
      'Visitor ID',
      'Country',
      'Country Code',
      'Device Type',
      'Referrer',
      'Timestamp'
    ];

    const escapeCsv = (val) => {
      if (val === null || val === undefined) return '""';
      const str = String(val).replace(/"/g, '""');
      return `"${str}"`;
    };

    const rows = events.map((e) => [
      escapeCsv(e.id),
      escapeCsv(e.event_type),
      escapeCsv(e.opportunity_id || ''),
      escapeCsv(e.opportunity_title || ''),
      escapeCsv(e.page_path || ''),
      escapeCsv(e.session_id),
      escapeCsv(e.visitor_id),
      escapeCsv(e.country),
      escapeCsv(e.country_code),
      escapeCsv(e.device_type),
      escapeCsv(e.referrer),
      escapeCsv(e.created_at)
    ].join(','));

    return [headers.join(','), ...rows].join('\r\n');
  },

  /**
   * Reset / clear analytics storage
   */
  clearAnalyticsData() {
    if (typeof localStorage !== 'undefined') {
      localStorage.removeItem('ath_analytics_events');
    }
  },

  /**
   * Seed realistic baseline activity if storage is initially blank
   */
  seedInitialAnalyticsIfEmpty() {
    if (typeof localStorage === 'undefined') return;

    try {
      const existing = localStorage.getItem('ath_analytics_events');
      if (existing && existing !== '[]') return;

      const opps = this.getOpportunities(true);
      const oppIds = opps.map((o) => ({ id: o.id, title: o.title }));
      if (oppIds.length === 0) return;

      const countries = [
        { name: 'Nigeria', code: 'NG', weight: 45 },
        { name: 'Ghana', code: 'GH', weight: 18 },
        { name: 'Kenya', code: 'KE', weight: 14 },
        { name: 'South Africa', code: 'ZA', weight: 10 },
        { name: 'Rwanda', code: 'RW', weight: 5 },
        { name: 'United States', code: 'US', weight: 5 },
        { name: 'United Kingdom', code: 'GB', weight: 3 }
      ];

      const devices = [
        { type: 'Desktop', weight: 55 },
        { type: 'Mobile', weight: 38 },
        { type: 'Tablet', weight: 7 }
      ];

      const referrers = ['Direct', 'https://google.com', 'https://linkedin.com', 'https://twitter.com', 'Internal Navigation'];

      const pickWeighted = (items) => {
        const total = items.reduce((acc, i) => acc + i.weight, 0);
        let rand = Math.random() * total;
        for (let i = 0; i < items.length; i++) {
          if (rand < items[i].weight) return items[i];
          rand -= items[i].weight;
        }
        return items[0];
      };

      const seedEvents = [];
      const now = Date.now();
      const visitorPool = Array.from({ length: 42 }, (_, i) => `vis_seed_${(i + 1).toString().padStart(3, '0')}`);
      const sessionPool = Array.from({ length: 70 }, (_, i) => `sess_seed_${(i + 1).toString().padStart(3, '0')}`);

      // Generate ~140 realistic past events distributed over the last 7 days
      for (let dayOffset = 6; dayOffset >= 0; dayOffset--) {
        const eventsPerDay = 18 + Math.floor(Math.random() * 14);
        const dayStart = now - dayOffset * 24 * 60 * 60 * 1000;

        for (let j = 0; j < eventsPerDay; j++) {
          const eventTime = new Date(dayStart + Math.floor(Math.random() * 24 * 60 * 60 * 1000)).toISOString();
          const visitor = visitorPool[Math.floor(Math.random() * visitorPool.length)];
          const session = sessionPool[Math.floor(Math.random() * sessionPool.length)];
          const country = pickWeighted(countries);
          const device = pickWeighted(devices);
          const opp = oppIds[Math.floor(Math.random() * oppIds.length)];
          const referrer = referrers[Math.floor(Math.random() * referrers.length)];

          const randType = Math.random();
          let type = 'page_view';
          let oppId = null;
          let oppTitle = null;
          let path = '#home';

          if (randType < 0.35) {
            type = 'page_view';
            path = Math.random() > 0.4 ? '#opportunities' : '#home';
          } else if (randType < 0.65) {
            type = 'opportunity_impression';
            oppId = opp.id;
            oppTitle = opp.title;
            path = '#opportunities';
          } else if (randType < 0.82) {
            type = 'opportunity_click';
            oppId = opp.id;
            oppTitle = opp.title;
            path = '#opportunities';
          } else if (randType < 0.94) {
            type = 'opportunity_view';
            oppId = opp.id;
            oppTitle = opp.title;
            path = `#opportunities/${opp.id}`;
          } else {
            type = 'apply_click';
            oppId = opp.id;
            oppTitle = opp.title;
            path = `#opportunities/${opp.id}`;
          }

          seedEvents.push({
            id: `evt_seed_${seedEvents.length + 1}`,
            event_type: type,
            opportunity_id: oppId,
            opportunity_title: oppTitle,
            page_path: path,
            session_id: session,
            visitor_id: visitor,
            country: country.name,
            country_code: country.code,
            referrer,
            device_type: device.type,
            created_at: eventTime
          });
        }
      }

      seedEvents.sort((a, b) => new Date(a.created_at).getTime() - new Date(b.created_at).getTime());
      localStorage.setItem('ath_analytics_events', JSON.stringify(seedEvents));
    } catch (e) {
      console.warn('Initial analytics seeding bypassed:', e);
    }
  }
};

